const CONTEXT_TYPE: string = "validationContext";
const STATE_TYPE: string = "state";
const STATE_RELATION: string = "hasState";

const VALID_NODE_NAME: string = "valid";
const INVALID_NODE_NAME: string = "invalid";

export {
  CONTEXT_TYPE,
  STATE_TYPE,
  STATE_RELATION,
  VALID_NODE_NAME,
  INVALID_NODE_NAME
};
