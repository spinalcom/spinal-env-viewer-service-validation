const CONTEXT_TYPE: string = "validationContext";

const STATE_TYPE: string = "validationState";
const STATE_RELATION: string = "hasValidationState";

const RECORD_TYPE: string = "validationRecord";
const RECORD_RELATION: string = "hasValidationRecord";

const VALID_NODE_NAME: string = "valid";
const INVALID_NODE_NAME: string = "invalid";

export {
  CONTEXT_TYPE,
  STATE_TYPE,
  STATE_RELATION,
  RECORD_TYPE,
  RECORD_RELATION,
  VALID_NODE_NAME,
  INVALID_NODE_NAME
};
