const VALIDATION_CONTEXT_TYPE: string = "validationContext";

const STATE_TYPE: string = "validationState";
const STATE_RELATION: string = "hasValidationState";

const RECORD_TYPE: string = "validationRecord";
const RECORD_RELATION: string = "hasValidationRecord";

const VALID_STATE_NAME: string = "valid";
const INVALID_STATE_NAME: string = "invalid";

export {
  VALIDATION_CONTEXT_TYPE,
  STATE_TYPE,
  STATE_RELATION,
  RECORD_TYPE,
  RECORD_RELATION,
  VALID_STATE_NAME,
  INVALID_STATE_NAME
};
